name: Collect Air Quality Data

on:
  schedule:
    # 每半小時執行一次，分鐘設為 0 和 30
    - cron: '0,30 * * * *'
  workflow_dispatch:  # 允許手動觸發

jobs:
  collect-data:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests pandas

      - name: Collect and process air quality data
        run: |
          python3 air_quality.py
          
      - name: Commit and push changes
        run: |
          git config --global user.name 'GitHub Action Bot'
          git config --global user.email 'action@github.com'
          git add air_quality_data/*.csv air_quality_combined.csv
          git commit -m "Update air quality data at $(date +'%Y-%m-%d %H:%M:%S')" || echo "No changes to commit"
          git push
